# 🖥️ 화면 OS 레벨 회전 설정 (실제 성공 방법)

> **검증 완료**: 라즈베리파이 4B (192.168.0.27)
> **작성일**: 2025-11-30  
> **상태**: ✅ 작동 확인

---

## ✅ 핵심 요약 (2단계만 필요)

화면을 세로로 회전시키는데 **필수적으로 필요한 것은 2가지**입니다:

1. **Wayland → X11 전환** (가장 중요!)
2. **xrandr 회전 명령어**

---

## 📝 설정 단계

### 1. Wayland → X11 전환 (필수!)

Wayland는 화면 회전 설정을 무시합니다. X11으로 전환해야 합니다.

```bash
# raspi-config로 X11 전환
sudo raspi-config nonint do_wayland W1

# 재부팅
sudo reboot
```

### 2. xrandr로 화면 회전

재부팅 후 SSH로 접속해서 실행:

```bash
# 화면을 90도(left) 회전
DISPLAY=:0 xrandr --output HDMI-1 --rotate left
```

**회전 옵션:**
- `normal` = 회전 없음 (가로)
- `left` = 90도 회전 (세로) ✅ 사용
- `inverted` = 180도 회전
- `right` = 270도 회전

### 3. 확인

```bash
# 현재 해상도 확인
DISPLAY=:0 xrandr | grep HDMI-1
```

**예상 출력:**
```
HDMI-1 connected primary 900x1600+0+0 left ...
```

---

## 🚀 자동화 (선택사항)

매번 수동으로 회전하기 싫다면 스크립트를 만들어 자동 실행:

### A. 회전 스크립트 생성

```bash
nano ~/.config/autostart/rotate-display.sh
```

**내용:**
```bash
#!/bin/bash
sleep 5
DISPLAY=:0 xrandr --output HDMI-1 --rotate left
```

권한 부여:
```bash
chmod +x ~/.config/autostart/rotate-display.sh
```

### B. 자동 실행 설정

```bash
mkdir -p ~/.config/autostart
nano ~/.config/autostart/rotate-display.desktop
```

**내용:**
```ini
[Desktop Entry]
Type=Application
Name=Rotate Display
Exec=/home/pi/.config/autostart/rotate-display.sh
Hidden=false
NoDisplay=false
X-GNOME-Autostart-enabled=true
```

---

## 🔧 트러블슈팅

### 문제: 화면이 여전히 가로 모드

**확인:**
```bash
# 1. X11으로 전환되었는지 확인
echo $XDG_SESSION_TYPE
# 출력: x11 (Wayland면 안 됨)

# 2. 수동으로 회전 명령어 실행
DISPLAY=:0 xrandr --output HDMI-1 --rotate left
```

### 문제: 부팅 시 자동 회전 안 됨

자동 실행 스크립트가 제대로 설정되었는지 확인:
```bash
ls -la ~/.config/autostart/
cat ~/.config/autostart/rotate-display.desktop
```

---

## ❌ 작동하지 않는 방법들 (시도 불필요)

다음 방법들은 **이 환경에서 작동하지 않습니다**:

### `/boot/config.txt` 설정들 (작동 안 함)
```ini
# 이것들은 작동하지 않음
display_rotate=1
display_lcd_rotate=1
display_hdmi_rotate=1
lcd_rotate=1
```

### 터치스크린 드라이버 (불필요)
```ini
# GT911 드라이버 - USB가 연결되지 않으면 불필요
dtoverlay=gt911
```

### X11 libinput 설정 (불필요)
터치 입력이 없으면 좌표 보정도 불필요합니다.

---

## 📚 참고사항

### 왜 이 방법만 작동하는가?

1. **Wayland 문제**: 최신 Raspberry Pi OS는 기본적으로 Wayland를 사용하는데, Wayland는 `/boot/config.txt`의 회전 설정을 무시합니다.

2. **X11 필수**: xrandr는 X11에서만 작동하므로, 반드시 X11으로 전환해야 합니다.

3. **동적 회전**: xrandr는 런타임에 화면을 회전시키므로 재부팅 없이 즉시 적용됩니다.

### 추가 명령어

```bash
# 모든 출력 장치 보기
DISPLAY=:0 xrandr

# 특정 해상도로 변경
DISPLAY=:0 xrandr --output HDMI-1 --mode 1920x1080

# 회전 제거 (원래대로)
DISPLAY=:0 xrandr --output HDMI-1 --rotate normal
```

---

**최종 검증**: 2025-11-30  
**성공 확인**: 라즈베리파이 4B (192.168.0.27)  
**프로젝트**: 운동복/수건 대여 시스템  
**화면 해상도**: 900x1600 (세로 모드)


